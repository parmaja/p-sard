/*
  This examples are worked, and this comment will ignored, not compiled or parsed as we say.
*/

//Single Line comment
CalcIt:Integer(p1, p2){
    :=p1 * p2 / 2;
  };

x := {
      y := 0;
      x := CalcIt(x, y);
      := y + x+ 500 * %10; //this is a result return of the block
  }; //do not forget to add ; here

f := 10.0;
f := z + 5.5;

{* Embeded block comment *};
{? Preprocessor Commands ?};

:= "Result:" + x + ' It is an example:
  Multi Line String
';