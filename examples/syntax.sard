/*
  This example of SARD script
*/

//Single Line comment

foo: integer(p1, p2) {
    f1: integer=10;
    :=p1 * p2 / 2;
  };

x := {
      y := 0;
      x := CalcIt(x, y);
      := y + x+ 500 * %10; //this is a result return of the block
  }; //do not forget to add ; here

{* Embeded block comment *};

f := 10.0;
f := z + 5.5;


{?name Preprocessor Commands:
      need to install addon to ur environment ?};

:= "Result:" + x + ' It is an example:
  Multi Line String
';